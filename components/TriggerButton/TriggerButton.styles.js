import { StyleSheet } from "react-native";

export default StyleSheet.create({
  triggerButton: {
    backgroundColor: "#000",
    borderRadius: 5,
    padding: 10,
    paddingHorizontal: 20,
    margin: 20,
    alignSelf: "center",
  },
  triggerButtonText: {
    fontSize: 20,
    color: "#fff",
  },
});
