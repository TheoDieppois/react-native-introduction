import { StyleSheet } from "react-native";

export default StyleSheet.create({
  buttonContainer: {
    alignItems: "center",
    gap: 5,
  },

  buttonCircle: {
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#1D55F2",
    height: 40,
    width: 40,
    borderRadius: 30,
  },
});
